import * as _Core from '@wrserver/core';
import * as _Crypt from '@wrserver/crypt';
import * as _Mail from '@wrserver/mail';
import * as _Data from '@wrserver/data';
import * as _Roles from '@wrserver/roles';
import * as _Auth from '@wrserver/auth';

export namespace WRServer {
	export const Core = _Core;
	export const Crypt = _Crypt;
	export const Mail = _Mail;
	export const Data = _Data;
	export const Roles = _Roles;
	export const Auth = _Auth;
}